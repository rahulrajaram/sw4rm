# SW4RM Python SDK — pinned runtime deps for running clients/examples
# These match pyproject.toml and are sufficient to run the examples
# without needing to regenerate protobuf stubs.

protobuf>=4.23,<5
grpcio>=1.56,<2
googleapis-common-protos>=1.60,<2

# Optional — only needed if you want to (re)generate protobuf stubs locally.
# Including this here ensures a single `pip install -r requirements.txt`
# equips both runtime and dev flows without extra steps.
grpcio-tools>=1.56,<2
